"""PillBot - AI-assisted home medication organizer, reminder, and dispenser.

Kept intentionally minimal so importing a single submodule (e.g. pillbot.safety)
does not trigger the app's import-time side effects. Run the app via
`python -m pillbot` or the `pillbot` console script (-> pillbot.app:main).
"""
__version__ = "0.1.0"
